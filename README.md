# Sprint7_project
My test project
## HECHO POR:
Juan Aranda 
## COMO SE HIZO:
Primero empezamos leyendo lo que debia realizar el primero paso decia que empezará por hacer un nuevo repositorio en Github y lo clonará en VScode, para eso cree una carpeta llamada "SPRINT7_PROJECT" y ahí lo guarde , ya que lo cloné creé un archivo de texto llamado "requerimentes.txt", en el cual escribí las librerias que ocupamos que son "Pandas", "plotly_express", "streamlit".
Despues de eso descargue el archivo que nos proporcionaron en la plataforma ("vehicles_us.csv") el cual nos daba datos para poder analizarlos y utilizarlos. Acabado esto creé una  nueva carpeta dentro de la que, estoy usando("SPRINT7_PROJECT"), llamada "notebooks" ahi creé un archivo llamado "EDA.ipynb" en el que mediante codigo pudimos abrir nuestro archivo csv ("vehicles_us.csv") y hacer un histograma, una vez que se ejecuto de manera correcta proseguí a crear un nuevo archivo llamado "App.py"en el meti el codigo que nos daban pero lo modifique porque solo tenia el codigo para hacer un histograma asi que copie ese codigo y lo pegue abajo pero cambiandolo para que el copiado de abajo me mostrara un grafico de dispercion, ahora lo que siguio fue hacer commit a todo lo que ya habia realizado.
Ahora venia la tarea de subir nuestro repositorio a Render, asi que abrimos la pagina de render y le dimos a crear uno nuevo y que fuera un "Servicio Web" y configure el servicio en la parte de "Build Command" escribimos el comando de la plataforma ("pip install --upgrade pip && pip install -r requirements.txty")y en Start Command lo mismo("streamlit run app.py"), lo desplegamos y esperamos a que estuviera listo, ya que funciono lo guardamos y ya acabamos.

## link Render
https://sprint7-project-ebu3.onrender.com

## link Github
https://github.com/JEAH-9/Sprint7_project.git
